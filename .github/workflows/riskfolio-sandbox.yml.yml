name: Riskfolio-Lib Optimizer Sandbox

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  riskfolio-sandbox:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    env:
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
      FINMIND_TOKEN: ${{ secrets.FINMIND_TOKEN }}
      TWELVE_DATA_API_KEY: ${{ secrets.TWELVE_DATA_API_KEY }}
      SUPABASE_TABLE: portfolio_db
      PORTFOLIO_ROW_ID: "1"

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install base dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Install Riskfolio-Lib dependencies
        run: |
          pip install -r requirements-riskfolio.txt

      - name: Probe Riskfolio-Lib dependency
        run: |
          python scripts/check_riskfolio_deps.py

      - name: Run Riskfolio-Lib sandbox
        run: |
          python scripts/run_riskfolio_sandbox.py

      - name: Upload sandbox artifact
        uses: actions/upload-artifact@v4
        with:
          name: riskfolio-sandbox-output
          path: |
            data/optimizer/riskfolio_sandbox_latest.json
            data/optimizer/riskfolio_sandbox_summary.md

      - name: Commit sandbox output
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/optimizer/riskfolio_sandbox_latest.json data/optimizer/riskfolio_sandbox_summary.md
          git diff --cached --quiet || git commit -m "Update Riskfolio sandbox output"
          git push
