# Manual Approval Input Layer

`config/manual_approval_override.json` is a user-editable review input file. It can only affect review-only draft generation and cannot enable broker submission, execution, or trade signals.
